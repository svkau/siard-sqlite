#!/usr/bin/env python3
"""
SIARD to SQLite Converter CLI (alternative name)
"""
import sys
from pathlib import Path

# Add the current directory to Python path so we can import siard_converter
script_dir = Path(__file__).parent
sys.path.insert(0, str(script_dir))

from siard_converter import main

if __name__ == '__main__':
    main()